<% if user_signed_in? %>
	<% if @favorite %>
  	<%= link_to "unfavorite", favorite_todo_path(id: todo_list), method: :delete %>
	<% else %>
  	<%= link_to "favorite", favorite_todos_path(id: todo_list), method: :post %>
	<% end %>
<% end %>
