<% if current_user %>
	<% if @favorite %>
  	<%= link_to "unfavorite", favorite_todos_path(todo_list), method: :destroy %>
	<% else %>
  	<%= link_to "favorite", favorite_todos_path(id: todo_list), method: :post %>
	<% end %>
<% end %>