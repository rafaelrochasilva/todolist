<div class="todo">
	<div class="card">
		<a href="<%= todo_list_path(todo_list) %>">
			<%= image_tag(todo_list.photo) %>

			<div class="todo-main">
				<div class="title" id="<%= todo_list.id %>"><%= todo_list.name %></div>
			</div>
		 </a>
	</div>
</div>